import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Keep the demo build strict but simple; images are CSS-only for now.
  reactStrictMode: true,
};

export default nextConfig;
